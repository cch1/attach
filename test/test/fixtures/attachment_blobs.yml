<%
def fixture_data(name)
  fn = File.join('test', 'fixtures', 'attachments', name)
  data = File.open(fn,'rb').read
  "!binary | \n    #{[data].pack('m').gsub(/\n/,"\n    ")}"
end
%>

one:
  blob: <%= fixture_data('rails.png') %>
  attachment_id: one
one_thumb:
  blob: <%= fixture_data('rails.png') %>
  attachment_id: one_thumbnail
two:
  blob: <%= fixture_data('AlexOnBMW#4.jpg') %>
  attachment_id: two
sss:
  blob: <%= fixture_data('SperrySlantStar.bmp') %>
  attachment_id: sss